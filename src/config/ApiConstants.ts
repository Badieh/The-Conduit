const BaseUrl = "https://conduit-realworld-example-app.fly.dev/api";

const loginUrl = `/users/login`;
const registerUrl = `/users`;

const updateUserUrl = `/user`;

const articlesUrl = `/articles`;

const yourFeedUrl = `/articles/feed`;

const tagsUrl = `/tags`;

export {
  BaseUrl,
  loginUrl,
  registerUrl,
  updateUserUrl,
  articlesUrl,
  yourFeedUrl,
  tagsUrl,
};
